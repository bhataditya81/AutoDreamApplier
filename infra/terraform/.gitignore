# Terraform state and secrets — NEVER commit
*.tfstate
*.tfstate.backup
*.tfvars
.terraform/
# .terraform.lock.hcl should be committed — tracks provider version selections
crash.log
override.tf
override.tf.json
